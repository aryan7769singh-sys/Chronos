import { ComingSoon } from "@/components/common/ComingSoon";

export const metadata = { title: "Calendar — Chronos" };

export default function CalendarPage() {
  return <ComingSoon title="Calendar" />;
}
