import { ComingSoon } from "@/components/common/ComingSoon";

export const metadata = { title: "Settings — Chronos" };

export default function SettingsPage() {
  return <ComingSoon title="Settings" />;
}
